%h1 All Movies

%table#movies
  %thead
    %tr
      %th 
      %th Movie Title
      %th Rating
      %th Release Date
      %th More Info
  %tbody
    - @movies.each_with_custom_index(1,1) do |movie, index|
      %tr
        %td=index
        %td=movie.title
        %td=movie.rating
        %td=movie.created_at
        %td=link_to "More about #{movie.title}", movie_path(movie)
        
= link_to 'Add new movie', new_movie_path